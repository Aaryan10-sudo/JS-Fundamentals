/* 
let age = 20;
let op = age > 18 ? "Can enter a bar" : "Cannot enter bar";
console.log(op);
 */
//Ternary operator evaluates a condition and executes a block of code based on the condition

let name = ["aaryan", "ram", "hari"];
let test = name.includes("aaryan");
let output = test === true ? "recognized" : "not recognized";
console.log(output);
